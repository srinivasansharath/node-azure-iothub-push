# Telemetry Push to Azure

Use the following files for each service:
mongodb.js: to push data to Azure Cosmos db account with mongo db apis
iotcental.js: to push data to an iothub or iotcentral service

